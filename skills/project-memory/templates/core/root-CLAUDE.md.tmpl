# [ПРОЕКТ]

Единое переносимое ядро проекта — в `[ЯДРО]/` (project-memory [ДАТА]).
Не создавай рядом отдельное ядро для другой модели.

Начало сессии: прочитать `[ЯДРО]/[ПРАВИЛА]`, затем верх
`[ЯДРО]/ЖУРНАЛ СЕССИЙ.md` и `[ЯДРО]/STATUS.md`.
Пути в документах — относительные от этой папки.
